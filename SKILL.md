---
name: story-to-video
description: 根据用户提供并确认的故事文案和当次参考图，自动生成旁白音频、音频时间轴、分镜提示词、批量插图、动态剪辑、3:4 竖版封面、4:3 横版封面、标题、简介、话题标签和平台上传版 MP4。用于把一个故事完整生产成可直接发布的视频素材包；题材、人物、年代和画风均随本次输入动态变化，不固定主人公、系列或模板。
---

# Story to Video

把“已锁定文案”和“本次参考图”视为唯一内容与视觉来源。不要把过往项目的人物、题材、年代、封面或图片带入新任务。

## 开始前必读

- 每次生产都读 `references/production-workflow.md`。
- 分析参考图、决定画面类型或写图片提示词时读 `references/visual-adaptation.md`。
- 制作片头时读 `references/opening-motion.md`。
- 导出时读 `references/delivery-presets.md`。
- 制作双封面和发布文案时读 `references/publishing-package.md`。
- 同时遵守 `hyperframes`、`hyperframes-core`、`hyperframes-animation`、`hyperframes-cli` 与 `media-use`。

## 输入合同

至少取得：

1. 已经确认、不可擅自改写的最终旁白文案；
2. 本次任务的一张或多张参考图；
3. 输出目录。

声音、封面、字幕或平台规格未指定时使用本 Skill 默认值。参考图缺失时，只有用户明确允许自行决定画风才能继续。

先判断每张参考图的作用：

- 文案需要持续出现的主角且参考图包含该角色：同时作为角色锚点和风格锚点；
- 文案没有固定主角，或参考图主体与文案无关：只作为风格锚点；
- 多张图：分别记录角色、画风、构图或封面用途，不要混为一个固定模板。

## 不可固定的内容

- 不固定猫、人、动物或任何主人公；
- 不固定“人生副本”、网络攻防、金融、王朝等题材；
- 不固定年代、地域、服装、道具、色板或绘画媒介；
- 不固定配音模型、声音 ID、片名、系列名、封面版式或话题标签；
- 不在 Skill 中保存用户项目的参考图、母图或封面图；
- 不把一条视频的生成图片当作下一条视频的默认参考。

## 可以固定的生产纪律

- 默认 16:9、1920×1080、30fps；
- 旁白驱动时间轴，先完成音频再决定镜头长度；
- 默认每分钟约 5–7 张图，单图通常 7–14 秒，按语义转折而不是机械等分；
- 静态图只做克制的推近、拉远、横移、斜移、局部特写和短叠化；
- 片头不单独添加铃声、冲击声或故障音效；旁白从 0 秒开始；
- 没有用户明确要求时，不添加高存在感 BGM 或正文音效；
- 默认只交付平台上传版，不额外保留超大高码率母版。
- 默认必须交付 3:4 竖版封面、4:3 横版封面、最终标题、简介和话题标签，不把发布文案留给用户补写。

## 执行顺序

1. 创建独立任务目录并复制本次输入，禁止覆盖其他任务。
2. 从参考图提取可复现的视觉规范，写入 `style_spec.json`。
3. 生成并锁定旁白，取得词级或句级时间戳。
4. 按题材选择画面语法，生成 `shot_timeline.json` 和逐镜提示词。
5. 批量生成图片并逐批自检，再做全局风格、人物和错图检查。
6. 用实际图片和旁白制作动态片头、正文和字幕。
7. 运行 HyperFrames 检查，渲染后从实际 MP4 抽查开头、中段、结尾和明显转场。
8. 使用 `scripts/encode-platform.mjs` 生成上传版并验证编码、响度、同步、时长和文件大小。
9. 根据本次视频重构并检查 3:4 与 4:3 两张封面，写出可直接复制的标题、简介和话题标签。
10. 把成片、双封面和发布文案放进同一个交付目录。

生成超过 30 张图片且运行环境允许并行时，最多使用 3 个隔离的图片生产者。每个生产者只写自己的批次目录，必须核对张数并打开接触表自检；主控仍需做跨批次一致性检查。

## 完成条件

- 成片内容忠于锁定文案；
- 全片视觉语言来自本次参考图，且同一条视频内部统一；
- 题材需要的画面类型正确，不把所有内容强行画成角色生活照；
- 片头从本次画面和标题动态生成，不含额外片头音效；
- 没有遗留其他项目的人物、图片、标题或路径；
- 成片可播放、音画同步、平台规格正确、文件大小合理；
- 竖版封面为 3:4，横版封面为 4:3；两张图不是机械裁切，缩略图下标题仍清晰；
- 发布文案包含一个无需用户再选择的最终标题、简介和恰好 10 个相关话题标签。
